doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
      h1 Products
        p This is a products page
        ul 
          each product in products
            p= product.name
            p= product.description
            p= product.price
        